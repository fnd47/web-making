# web-making
this is my first project about front end web. is an Audi fanbased website, modern styling with landing page integrated

- u have to create a folder named (css) because the directory is in "css/styles.css" although u can still change the index.html into "style.css" only
- this the directory looks like (inline 17) ( <link rel="stylesheet" href="css/styles.css" /> )u can change into ( <link rel="stylesheet" href="styles.css" />  )if u dont want to create a separate folder
